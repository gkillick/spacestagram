import { StyleGridItemDirective } from './style-grid-item.directive';

describe('StyleGridItemDirective', () => {
  it('should create an instance', () => {
    const directive = new StyleGridItemDirective();
    expect(directive).toBeTruthy();
  });
});
